// 배열선언1- 빈배열 선언 후 값 할당
let str=[];
//자바스크립트는 문자형은 배열로 다룸
str = '선문아그만자';
document.write(str);
document.write(str[0]+str[1]+str[2]+str[str.length-1]+'<hr>');
//배열의 총길이
document.write(str.length); //6
console.log(str);